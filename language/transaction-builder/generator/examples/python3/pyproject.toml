[tool.poetry]
name = "diem-example"
version = "0.1.0"
description = "An example Python project for language bindings"
license = "Apache-2.0"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"
numpy = "^1.20"
# diem_types is generated by generate-transaction-builders.

[tool.poetry.dev-dependencies]
pyre-check = "^0.0.59"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
